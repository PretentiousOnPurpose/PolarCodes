#include <iostream>
#include "utils.hpp"

int * polarEncoding(int * kronKer, int * data);
int * setFrozenDataBits(int * bitData, int * relSeq);
int * SCDecoding(int * recvData);
int * BPDecoding(int * recvData);
int * GNNDecoding(int * recvData);
